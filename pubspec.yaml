name: solid
description: >
  A lightweight, reactive state management library for Flutter.
  Inspired by SolidJS signals – fine-grained reactivity without code generation.
version: 1.0.0
repository: https://github.com/your-org/solid

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
